// In JS we can able to change the properpties of an object once its assiged.
var user = {name: "Alice", "age": 30};

console.log(user);

user = {age:45};

console.log(user);

user = {age:45};

console.log(user);

user = {address: {"city": "New York", "country": "USA"}};

console.log(user);