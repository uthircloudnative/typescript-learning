var salries:number[] = [200, 300, 400, 500, 600];

console.log(salries)

var names:String[] = [ 'John', 'Doe', 'Jane', 'Smith'];

console.log(names)
// Will result in error as we are assigning string in number array.
// In JS we can change the data type of array elements at any time.

//salries = ['400', '500', '600']; // This will throw an error as we are trying to assign string array to number array.